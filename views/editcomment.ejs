<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.0/normalize.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css">
    <title>Fake Instagram</title>
</head>

<body>
    <header>
        <h1 style="display: inline;"><a href="/">Fake Instagram</a></h1>
        <nav style="display: inline;">
            <% if(user) { %>
            <p>Welcome <%=user.username%></p>
            <a href="/users/logout">Log out</a>
            <% } else { %>
            <a href="/users/login">Log in</a>
            <a href="/users/signup">Sign up</a>
            <% } %>
        </nav>
    </header>
    <main>
        <h2>Edit Comment</h2>
        <form action="/<%= posting._id %>/comment/edit/<%= comment %>?_method=PUT" method="POST">
            <textarea name="comment" value="/<%= posting._id %>/comment/edit/<%= comment %>"></textarea><br />
            <input type="submit" value="Edit comment">
        </form>
    </main>
</body>

</html>